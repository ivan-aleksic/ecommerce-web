import { WebShopValidators } from './web-shop-validators';

describe('WebShopValidators', () => {
  it('should create an instance', () => {
    expect(new WebShopValidators()).toBeTruthy();
  });
});
